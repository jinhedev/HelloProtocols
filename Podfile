# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'HelloProtocols' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HelloProtocols
    pod 'Parse'
    pod 'Locksmith'

  target 'HelloProtocolsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'HelloProtocolsUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
